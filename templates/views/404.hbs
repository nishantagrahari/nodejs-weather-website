<html>
    
   <head>
       <title>
        {{title}} page
       </title>
        <link rel="stylesheet" href="./css/style.css">
    </head>

    <body>
       <div class='main'>
            {{>header}}
           <p>{{errorMessage}}</p>
        </div> 
        {{>footer}}
    </body>
</html>